<UserControl x:ConnectionId='1'
    x:Class="BaseLogic.Xaml_Controls.DisplayNewsItemControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XamlControlLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"                                                    >

    <Grid>
        <ScrollViewer x:Name="ContentScrollViewer" VerticalScrollMode="Auto" Background="White">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="{Binding Title}" Margin="4,0" Foreground="{Binding TitleColor}" FontSize="35" FontWeight="Bold" TextWrapping="WrapWholeWords" />
                <TextBlock Grid.Row="1" Text="{Binding TimeStamp}" Visibility="{Binding TimeStampVisibilty}" Margin="4,0" Foreground="Black" FontSize="15" FontWeight="Thin" TextWrapping="WrapWholeWords" />
                <TextBlock Grid.Row="2" Text="{Binding ContentSummary}" Visibility="{Binding SummaryVisibilty}" Margin="{Binding ContentMargins}" Foreground="Black" FontSize="20" FontWeight="Bold" TextWrapping="WrapWholeWords" />
                <ListView Grid.Row="3" IsItemClickEnabled="False" IsHitTestVisible="False" ItemsSource="{Binding Body}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock  Text="{Binding}" Margin="4,5,4,15" Foreground="Black" FontSize="20" TextWrapping="WrapWholeWords" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock Grid.Row="4" HorizontalAlignment="Right" Text="{Binding Author}" Margin="4,5,4,15" Foreground="Black" FontSize="16" TextWrapping="WrapWholeWords" />

                <Grid x:ConnectionId='3' x:Name="MediaGrid"                       Grid.Row="5" Margin="4,0" HorizontalAlignment="Stretch" Height="Auto" Visibility="{Binding MediaVisibilty}">
                    <MediaElement x:ConnectionId='5' x:Name="Media" 
              Source="{Binding MediaFile}" 
              AutoPlay="False"                                                                       />
                    <Image x:Name="PlayImage" Visibility="Collapsed"  Source="/Assets/Play.png" Height="100" />
                </Grid>


                <ListView x:ConnectionId='4'
                x:Name="ImagesListview"
                Grid.Row="6"
                                    Margin="4,5"
                                    ItemsSource="{Binding ImageList}"
                    IsItemClickEnabled="True"
                                                        >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border BorderThickness="0" BorderBrush="Black" HorizontalAlignment="Stretch" VerticalAlignment="Center" MinHeight="50">
                                <Grid>
                                    <ProgressBar x:Name="DataProgressBar" Margin="5" IsIndeterminate="True" VerticalAlignment="Top"/>
                                    <Image Source="{Binding}" />
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                            <Setter Property="BorderThickness" Value="0"/>
                            <Setter Property="Margin" Value="0,10,0,0"/>
                            <Setter Property="Padding" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                </ListView>
                <WebView Grid.Row="7" Margin="4,5" Height="300" HorizontalAlignment="Stretch" Visibility="{Binding DisplayWebView}"  Source="{Binding YoutubeURL}"/>
            </Grid>
        </ScrollViewer>
        <ScrollViewer x:Name="FullImageScrollViewer" Visibility="Collapsed"
                  VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto" 
                  ZoomMode="Enabled">

            <Image x:ConnectionId='2' x:Name="FullImage"                          >

            </Image>
        </ScrollViewer>
    </Grid>
</UserControl>

