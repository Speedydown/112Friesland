<UserControl x:ConnectionId='1'
    Name="Control"
    x:Class="BaseLogic.Xaml_Controls.DisplayNewsItemControlWideScreen"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XamlControlLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"                                                    >

    <Grid x:Name="LayoutRoot">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid Background="{Binding TitleColorWindows}" Height="52">
            <TextBlock Grid.Row="0" Text="{Binding Title}" Margin="4,5" Foreground="White" FontSize="35" FontWeight="Bold" TextWrapping="WrapWholeWords" />
        </Grid>
        <ScrollViewer x:Name="ContentScrollViewer" Grid.Row="1" VerticalScrollMode="Auto" VerticalScrollBarVisibility="Auto" Background="White">
            <Grid Margin="0,0,20,0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="1" Text="{Binding TimeStamp}" Visibility="{Binding TimeStampVisibilty}" Margin="4,0" Foreground="Black" FontSize="15" FontWeight="Thin" TextWrapping="WrapWholeWords" />
                <TextBlock Grid.Row="2" Text="{Binding ContentSummary}" Visibility="{Binding SummaryVisibilty}" Margin="{Binding ContentMargins}" Foreground="Black" FontSize="20" FontWeight="Bold" TextWrapping="WrapWholeWords" />
                <ListView Grid.Row="3" IsItemClickEnabled="False" IsHitTestVisible="False" ItemsSource="{Binding Body}">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <TextBlock  Text="{Binding}" Margin="4,5,4,15" Foreground="Black" FontSize="20" TextWrapping="WrapWholeWords" />
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock Grid.Row="4" HorizontalAlignment="Right" Text="{Binding Author}" Margin="4,5,4,15" Foreground="Black" FontSize="16" TextWrapping="WrapWholeWords" />
                <WebView Grid.Row="5" Margin="4,5" Height="600" HorizontalAlignment="Center" Visibility="{Binding DisplayWebView}"  Source="{Binding YoutubeURL}"/>
            </Grid>
        </ScrollViewer>
        <ScrollViewer x:Name="ImageScrollViewer" VerticalAlignment="Bottom"  Grid.Row="2" HorizontalAlignment="Center" HorizontalScrollMode="Auto" VerticalScrollBarVisibility="Disabled" HorizontalScrollBarVisibility="Auto" VerticalScrollMode="Disabled" ZoomMode="Disabled">
            <ListView x:ConnectionId='4' x:Name="ImageListview" ScrollViewer.VerticalScrollMode="Disabled" ItemsSource="{Binding ImageList}"  SelectionMode="None"                                IsItemClickEnabled="True">
                <ListView.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Height="180" Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ListView.ItemsPanel>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <Border BorderThickness="0" BorderBrush="Black" HorizontalAlignment="Stretch" VerticalAlignment="Center" MinHeight="50">
                            <Grid>
                                <ProgressBar x:Name="DataProgressBar" Margin="5" IsIndeterminate="True" VerticalAlignment="Top"/>
                                <Image Source="{Binding}" Stretch="Fill" />
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </ScrollViewer>
        <Grid x:Name="ImageViewGrid" Grid.Row="1" Background="White" Visibility="Collapsed" VerticalAlignment="Stretch" HorizontalAlignment="Stretch">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <Image x:ConnectionId='2' x:Name="LargeImage"  Source="{Binding}" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"                      />
            <Button x:ConnectionId='3' x:Name="CloseButton" HorizontalAlignment="Right" VerticalAlignment="Top" Margin="35,10" Background="White"  Foreground="{Binding TitleColorWindows}" BorderBrush="{Binding TitleColorWindows}" Content="Sluiten"                          />
        </Grid>
    </Grid>
</UserControl>

