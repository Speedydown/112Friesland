<UserControl
    x:Class="BaseLogic.Xaml_Controls.LoadingControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XamlControlLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Visibility="{Binding ControlVisibility}">
        <ProgressBar x:Name="DataProgressBar" Margin="5,5,0,0" IsIndeterminate="True" VerticalAlignment="Top"/>
        <Grid x:Name="ErrorGrid" HorizontalAlignment="Center" VerticalAlignment="Center" >
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            <TextBlock x:Name="LoadingTextBlock" Foreground="{Binding TextColor}" Grid.Row="0" Text="{Binding LoadingText}" TextWrapping="WrapWholeWords" FontSize="20" TextAlignment="Center" />
            <TextBlock Grid.Row="0" Text="{Binding ErrorText}" Foreground="{Binding TextColor}" Visibility="{Binding ErrorTextVisibility}" TextWrapping="WrapWholeWords" FontSize="20" TextAlignment="Center" />
            <TextBlock Grid.Row="1" Text="{Binding ErrorSubText}" Foreground="{Binding TextColor}" Visibility="{Binding ErrorTextVisibility}" TextWrapping="WrapWholeWords" FontSize="12" TextAlignment="Center" Margin="0, 20" />
        </Grid>
    </Grid>
</UserControl>

