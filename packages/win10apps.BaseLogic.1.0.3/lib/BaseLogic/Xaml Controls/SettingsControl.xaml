<UserControl x:ConnectionId='1'
    x:Class="BaseLogic.Xaml_Controls.SettingsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:XamlControlLibrary"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"                                                     x:Name="_SettingsControl">

    <StackPanel  Margin="0,-1">
        <ListView  Margin="5" ItemsSource="{Binding Settings.ToggleSwitches}">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid Margin="0,-10,0,0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="100"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding Header}" Grid.Column="0" Foreground="{Binding ElementName=_SettingsControl,Path=Foreground}" FontSize="25" VerticalAlignment="Center" TextWrapping="WrapWholeWords" />
                        <ToggleSwitch IsOn="{Binding IsOn, Mode=TwoWay}" Foreground="{Binding ElementName=_SettingsControl,Path=Foreground}" Grid.Column="1" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackPanel>
</UserControl>

